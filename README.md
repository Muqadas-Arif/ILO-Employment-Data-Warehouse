# ILO-Employment-Data-Warehouse
This project demonstrates the first step of a data warehouse ETL pipeline for ILO employment data. Using SQL Server, we created a staging table to safely load raw CSV data. The code handles large datasets and stores all columns exactly as received, preserving the original information. 
